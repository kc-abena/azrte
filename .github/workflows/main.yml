name: Manual OIDC to ARM & Graph Tokens

on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  get-azure-tokens:
    runs-on: ubuntu-latest

    steps:
      - name: Get GitHub OIDC Token
        id: oidc
        run: |
          echo "🔑 Requesting GitHub OIDC Token..."
          OIDC_TOKEN=$(curl -s -H "Authorization: bearer $ACTIONS_ID_TOKEN" \
            "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=api://Azur
